[project]
name = "retail-data-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "abhirwt289@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
walmart-data-project = "retail_data_project:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
